{
  "name": "webhid-daemon-nm-host",
  "description": "WebHID daemon as native-messaging host (no separate forwarder)",
  "path": "{{DAEMON_BIN}}",
  "type": "stdio",
  "allowed_extensions": ["webhid@k4zoku.dev"]
}

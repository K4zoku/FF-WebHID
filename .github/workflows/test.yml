name: Test

on:
  push:
    branches: [ main ]
    paths:
      - 'crates/**'
      - 'addon/**'
      - 'tests/**'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'crates/**'
      - 'addon/**'
      - 'tests/**'
      - '.github/workflows/test.yml'
  workflow_dispatch:

jobs:
  unit:
    name: Unit Tests
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates -> target
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - name: Run tests
        run: cargo test --manifest-path crates/Cargo.toml

  test-browser:
    name: Browser-only Tests
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - name: Build addon
        run: |
          npm ci
          make build-addon
      - name: Install Playwright
        working-directory: tests
        run: |
          npm ci
          npx playwright install firefox --with-deps
      - name: Run browser-only tests
        working-directory: tests
        run: npx playwright test --project=firefox-browser

  # e2e:
  #   name: E2E Tests
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - uses: actions/checkout@v7
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: crates -> target
  #     - name: Install dependencies
  #       run: sudo apt-get update && sudo apt-get install -y libudev-dev
  #     - name: Build Rust binaries
  #       run: cargo build --manifest-path crates/Cargo.toml
  #     - name: Setup uhid + udev rules
  #       run: |
  #         sudo modprobe uhid
  #         sudo tee /etc/udev/rules.d/99-webhid-e2e.rules <<'EOF'
  #         KERNEL=="uhid", SUBSYSTEM=="misc", MODE="0666"
  #         KERNEL=="hidraw*", MODE="0666", TAG+="uaccess"
  #         EOF
  #         sudo udevadm control --reload-rules
  #         sudo udevadm trigger
  #     - name: Build addon
  #       run: |
  #         npm ci
  #         make build-addon
  #     - name: Install Playwright
  #       working-directory: tests
  #       run: |
  #         npm ci
  #         npx playwright install firefox --with-deps
  #     - name: Run E2E tests
  #       working-directory: tests
  #       run: npx playwright test --project=firefox-e2e

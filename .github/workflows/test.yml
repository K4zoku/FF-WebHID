name: Test

on:
  push:
    branches: [ main ]
    paths:
      - 'crates/**'
      - 'addon/**'
      - 'tests/**'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'crates/**'
      - 'addon/**'
      - 'tests/**'
      - '.github/workflows/test.yml'
  workflow_dispatch:

jobs:
  build:
    name: Build (debug)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates -> target
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - name: Build binaries
        run: cargo build --manifest-path crates/Cargo.toml
      - name: Upload binaries
        uses: actions/upload-artifact@v7
        with:
          name: debug-binaries
          path: |
            crates/target/debug/webhid-daemon
            crates/target/debug/webhid-native-messaging
            crates/target/debug/uhid-mock

  unit:
    name: Unit Tests
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates -> target
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - name: Run tests
        run: cargo test --manifest-path crates/Cargo.toml

  # e2e:
  #   name: E2E Tests
  #   needs: build
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - uses: actions/checkout@v7
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: crates -> target
  #     - name: Install dependencies
  #       run: sudo apt-get update && sudo apt-get install -y libudev-dev
  #     - name: Download binaries
  #       uses: actions/download-artifact@v8
  #       with:
  #         name: debug-binaries
  #         path: crates/target/debug/
  #     - name: Fix permissions
  #       run: chmod +x crates/target/debug/webhid-daemon crates/target/debug/webhid-native-messaging crates/target/debug/uhid-mock
  #     - name: Setup uhid + udev rules
  #       run: |
  #         sudo modprobe uhid
  #         sudo tee /etc/udev/rules.d/99-webhid-e2e.rules <<'EOF'
  #         KERNEL=="uhid", SUBSYSTEM=="misc", MODE="0666"
  #         KERNEL=="hidraw*", MODE="0666", TAG+="uaccess"
  #         EOF
  #         sudo udevadm control --reload-rules
  #         sudo udevadm trigger
  #     - name: Build addon
  #       run: |
  #         npm ci
  #         make build-addon
  #     - name: Install Playwright
  #       working-directory: tests/e2e
  #       run: |
  #         npm ci
  #         npx playwright install firefox
  #     - name: Run E2E tests
  #       working-directory: tests/e2e
  #       run: npx playwright test

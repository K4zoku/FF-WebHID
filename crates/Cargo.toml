[workspace]
members = [
    "webhid",
    "webhid-daemon",
    "webhid-native-messaging",
]
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
